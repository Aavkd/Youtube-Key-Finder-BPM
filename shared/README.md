# /shared

Optional home for **OpenAPI-generated** types/clients shared by the frontend.

The frontend generates its typed API client from the backend's OpenAPI schema
(served at `http://localhost:8000/openapi.json`); do not hand-duplicate types.
Generation is wired up in **Phase 5**. This directory is a placeholder until
then.
