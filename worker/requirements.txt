# Worker runtime deps.
pydantic==2.10.4
pydantic-settings==2.7.1

# Database (mirrors the backend; talks to the same Postgres the backend migrates).
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
greenlet==3.1.1

# Download (ffmpeg is installed in the Docker image for conversion).
yt-dlp>=2025.5.1

# Analysis — Phase 3 (lazily imported; not needed for tests).
numpy==1.26.4
scipy==1.13.0
soundfile==0.12.1
librosa==0.10.2
essentia==2.1b6.dev1389

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
